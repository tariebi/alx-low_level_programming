#include "main.h"
#include <stdio.h>
/**
 *  _abs - function that computes the absolute value of an integeger
 *  @c : is the int that will use for the argument of the function
 *  return: 0
 */
int _abs (int c)
{
	if (c > 0 || c == 0)
	{
	retun (c) ;
	}
	else
	return (c * -1);
}
