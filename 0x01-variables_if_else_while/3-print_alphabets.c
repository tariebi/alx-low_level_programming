#include <stdio.h>

/**
 * main - Ommit some alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; c <='z'; ch++)
	{
		putchar (ch);
	}
	for (ch = 'A' ; ch <= 'Z' ; ch++)
		{
			putchar(ch);
		}

	putchar('\n');
	return (0);
}
